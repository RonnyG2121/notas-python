[project]
name = "web-scraping"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "scrapy>=2.14.1",
    "scrapy-playwright>=0.0.46",
]

[dependency-groups]
dev = [
    "ruff>=0.15.0",
]
